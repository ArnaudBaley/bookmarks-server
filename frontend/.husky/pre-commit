#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd "$(dirname -- "$0")/.." || exit

echo "🔍 Running linter..."
npm run lint:check || {
  echo "❌ Linting failed. Please fix the errors and try again."
  echo "💡 Tip: Run 'npm run lint' to auto-fix some issues."
  exit 1
}

echo "🧪 Running unit tests..."
npm run test:unit || {
  echo "❌ Unit tests failed. Please fix the errors and try again."
  exit 1
}

echo "✅ All checks passed!"
